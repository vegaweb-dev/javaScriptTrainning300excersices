// Declare a variable called `nombre` and assign it your name as a string.
// Then declare another variable called `edad` and assign it your age as a number.
// Finally, print the following message to the console (using concatenation or a template string):
// Hello, my name is [your name] and I am [your age] years old.

const introduce =(nombre,edad)=>console.log(`Hello, my neme is ${nombre} and I am ${edad}`)
export {introduce}